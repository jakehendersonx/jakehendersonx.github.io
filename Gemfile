source 'https://rubygems.org'

# Use the github-pages gem to ensure compatibility
gem 'github-pages', group: :jekyll_plugins

# Optional: additional Jekyll plugins you might want
group :jekyll_plugins do
  gem 'jekyll-feed'
  gem 'jekyll-seo-tag'
  gem 'jekyll-sitemap'
end